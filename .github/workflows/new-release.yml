name: NewRelease
on:
  push:
    branches:
      - dev
    paths:
      - 'GameModeManager/Plugin.cs'
jobs:
  NewRelease:
    if: true
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          fetch-depth: 2

      - name: Get Version Information
        id: version_info
        run: |
          echo "Getting current and previous version information..."

          CURRENT_MODULE_VERSION=$(grep -oP 'ModuleVersion => "\K[^"]+' GameModeManager/Plugin.cs)
          echo "current_module_version=$CURRENT_MODULE_VERSION" >> $GITHUB_OUTPUT # Changed to GITHUB_OUTPUT

          PREVIOUS_MODULE_VERSION=$(git show HEAD~1:GameModeManager/Plugin.cs | grep -oP 'ModuleVersion => "\K[^"]+' GameModeManager/Plugin.cs)
          echo "previous_module_version=$PREVIOUS_MODULE_VERSION" >> $GITHUB_OUTPUT # Changed to GITHUB_OUTPUT

          CURRENT_CONFIG_VERSION=$(grep -oP 'public int Version { get; set; } = \K\d+' GameModeManager/Config.cs)
          echo "current_config_version=$CURRENT_CONFIG_VERSION" >> $GITHUB_OUTPUT # Changed to GITHUB_OUTPUT

          PREVIOUS_CONFIG_VERSION=$(git show HEAD~1:GameModeManager/Config.cs | grep -oP 'public int Version { get; set; } = \K\d+' GameModeManager/Config.cs)
          echo "previous_config_version=$PREVIOUS_CONFIG_VERSION" >> $GITHUB_OUTPUT # Changed to GITHUB_OUTPUT

      - name: Check for Version Changes
        id: check_version_change
        run: |
          MODULE_VERSION_CHANGED="false"
          CONFIG_VERSION_CHANGED="false"

          if [[ "${{ steps.version_info.outputs.current_module_version }}" != "${{ steps.version_info.outputs.previous_module_version }}" ]]; then # Changed to use step output
            MODULE_VERSION_CHANGED="true"
          fi
          echo "module_version_changed=$MODULE_VERSION_CHANGED" >> $GITHUB_OUTPUT # Changed to GITHUB_OUTPUT

          if [[ "${{ steps.version_info.outputs.current_config_version }}" != "${{ steps.version_info.outputs.previous_config_version }}" ]]; then # Changed to use step output
            CONFIG_VERSION_CHANGED="true"
          fi
          echo "config_version_changed=$CONFIG_VERSION_CHANGED" >> $GITHUB_OUTPUT # Changed to GITHUB_OUTPUT

      - name: Get Commit History
        if: ${{ steps.check_version_change.outputs.module_version_changed == 'true' }} # Changed to use step output
        id: get_commit_history
        run: |
          LAST_TAG=$(git describe --tags --abbrev=0 2>/dev/null) || LAST_TAG=$(git rev-list --max-parents=0 HEAD) # Fallback to first commit if no tags
          COMMIT_HISTORY=$(git log --pretty=format:"%s" "$LAST_TAG"..HEAD)
          echo "commit_history=$(echo "$COMMIT_HISTORY" | jq -sRr @uri)" >> $GITHUB_OUTPUT # Changed to GITHUB_OUTPUT

      - name: Parse Commit History
        if: ${{ steps.check_version_change.outputs.module_version_changed == 'true' }} # Changed to use step output
        id: parse_commits
        run: |
          COMMIT_HISTORY="${{ steps.get_commit_history.outputs.commit_history }}" # Changed to use step output
          FEATURES=""
          BUG_FIXES=""
          MISC=""

          IFS=$'\n'
          for COMMIT in $(echo "$COMMIT_HISTORY"); do
            case "$COMMIT" in
              ADDED:*|Added:*)
                FEATURES="$FEATURES- $(echo "$COMMIT" | cut -d':' -f2-)\n"
                ;;
              Fix:*|Bug:*|BugFix:*)
                # Capture the "Thanks" and issue number if present
                THANKS=$(echo "$COMMIT" | grep -oE 'Thanks\s+@\w+\s+#\d+')
                MESSAGE=$(echo "$COMMIT" | sed -E 's/Fix:|Bug:|BugFix://i' | sed -E 's/Thanks\s+@\w+\s+#\d+//')
                BUG_FIXES="$BUG_FIXES- $(echo "$MESSAGE" | sed -e 's/^[[:space:]]*//') $THANKS\n"
                ;;
              *)
                MISC="$MISC- $COMMIT\n"
                ;;
            esac
          done

          if [[ -n "$FEATURES" ]]; then
            echo "features=$(echo "$FEATURES" | jq -sRr @uri)" >> $GITHUB_OUTPUT # Changed to GITHUB_OUTPUT
          fi
          if [[ -n "$BUG_FIXES" ]]; then
            echo "bug_fixes=$(echo "$BUG_FIXES" | jq -sRr @uri)" >> $GITHUB_OUTPUT # Changed to GITHUB_OUTPUT
          fi
          if [[ -n "$MISC" ]]; then
            echo "misc=$(echo "$MISC" | jq -sRr @uri)" >> $GITHUB_OUTPUT # Changed to GITHUB_OUTPUT
          fi

      - name: Setup .NET
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '8.0.x'

      - name: Restore dependencies
        run: dotnet restore GameModeManager.sln

      - name: Build solution
        run: dotnet build --configuration Debug GameModeManager.sln

      - name: Create ZIP archive
        if: ${{ steps.check_version_change.outputs.module_version_changed == 'true' }} # Changed to use step output
        run: |
          cd bin/Debug/net8.0/Public
          zip -r ../../../GameModeManager_v${{ steps.version_info.outputs.current_module_version }}.zip . # Changed to use step output

      - name: Create Release
        uses: actions/create-release@v1.1.4
        if: ${{ steps.check_version_change.outputs.module_version_changed == 'true' }} # Changed to use step output
        id: create_release
        env:
          GITHUB_TOKEN: ${{ secrets.TOKEN }}
        with:
          tag_name: v${{ steps.version_info.outputs.current_module_version }} # Changed to use step output
          release_name: Game Mode Manager v${{ steps.version_info.outputs.current_module_version }} # Changed to use step output
          body: |
            # Game Mode Manager v${{ steps.version_info.outputs.current_module_version }}
            ${{ steps.check_version_change.outputs.config_version_changed == 'true' && '> [!IMPORTANT]
            > - If moving from v${{ steps.version_info.outputs.previous_module_version }} or earlier, back up your `GameModeManager.json` configuration file and let the plugin generate the new version. This version has minor changes to the configuration file.
            >' || '' }}

            ${{ steps.parse_commits.outputs.features && '## Features' || '' }} # Changed to use step output
            ${{ steps.parse_commits.outputs.features }} # Changed to use step output

            ${{ steps.parse_commits.outputs.bug_fixes && '## Bug Fixes' || '' }} # Changed to use step output
            ${{ steps.parse_commits.outputs.bug_fixes }} # Changed to use step output

            ${{ steps.parse_commits.outputs.misc && '## Misc' || '' }} # Changed to use step output
            ${{ steps.parse_commits.outputs.misc }} # Changed to use step output

            ## Installation
            1. Install Metamod:Source and Counter Strike Sharp.
            2. Copy `addons` and `cfg` folders to `/csgo/`.
            3. Make sure your `gamemodes_server.txt` or custom map group file is in [VDF Format](https://developer.valvesoftware.com/wiki/VDF) and contains a list of map groups.
            5. Update each game mode configuration file (i.e. comp.cfg) to include `css_gamemode <mode>`.
            6. After the first run, update the configuration file `GameModeManager.json`.

            For more information on the configuration of these features, please see the updated [README.md](https://github.com/nickj609/GameModeManager/blob/main/README.md).

            **Full Changelog**: https://github.com/nickj609/GameModeManager/compare/v${{ steps.version_info.outputs.previous_module_version }}...v${{ steps.version_info.outputs.current_module_version }} # Changed to use step output
          draft: false
          prerelease: false

      - name: Upload Release Asset
        uses: actions/upload-release-asset@v1
        if: ${{ steps.check_version_change.outputs.module_version_changed == 'true' }} # Changed to use step output
        env:
          GITHUB_TOKEN: ${{ secrets.TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: GameModeManager_v${{ steps.version_info.outputs.current_module_version }}.zip # Changed to use step output
          asset_name: GameModeManager_v${{ steps.version_info.outputs.current_module_version }}.zip # Changed to use step output
          asset_content_type: application/zip